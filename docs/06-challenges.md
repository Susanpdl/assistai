# 06 · Challenges & Solutions

A running log of problems we hit during development and exactly how we solved them. *(This is
our "lessons learned" file — when something breaks and we figure it out, we write it here so we
don't lose the knowledge and don't trip over the same thing twice.)*

Each entry follows the same shape: **Symptom** (what we saw) → **Root cause** (why it happened)
→ **Fix** (what we did) → **Takeaway** (the lesson). Newest phase last.

---

## Setup · Publishing to GitHub + Netlify

### C1 · The git repository was rooted at the Desktop, not the project

- **Symptom:** Running `git status`/`git add -A` from inside `AssistAI/` showed files from
  *outside* the project (other folders on the Desktop) with `../` paths.
- **Root cause:** There was no `.git` inside `AssistAI/`. The nearest repository was at
  `~/Desktop`, so git treated the *entire Desktop* as the project. A normal `git add -A` +
  commit would have swept in personal files (tax PDFs, other projects) — and we were about to
  push to a **public** repo. *(Git walks up the folder tree until it finds a `.git`; if the
  project itself has none, it adopts whichever ancestor does.)*
- **Fix:** Gave the project its **own** repository — `git init -b main` *inside* `AssistAI/` —
  then verified with `git rev-parse --show-toplevel` that the repo root was now the project
  folder, not the Desktop. Committed only project files and pushed that.
- **Takeaway:** Before the first commit in a new project, always confirm
  `git rev-parse --show-toplevel` points at the project root. A nested repo shadows the
  ancestor, which is exactly what we want here.

### C2 · Netlify deploy can't be fully automated from the terminal

- **Symptom:** No way to complete the Netlify deploy headlessly — it needs a browser login.
- **Root cause:** Netlify authentication is interactive (OAuth in a browser); there was no
  Netlify CLI token configured locally.
- **Fix:** Chose **GitHub-connected continuous deploy** instead of CLI pushing. Added a
  `netlify.toml` (build `npm run build`, publish `dist`, SPA redirect) so that when the repo is
  linked in Netlify's UI, the build settings are detected automatically. The one manual step
  (authorizing GitHub in Netlify) is unavoidable but happens once.
- **Takeaway:** For static frontends, connecting the Git repo beats CLI deploys — every push
  redeploys, and the build config lives in version control (`netlify.toml`).

---

## Phase 0 · Foundations (backend scaffold + schema)

### C3 · Postgres port 5432 collision with a local install

- **Symptom:** Connecting to the Docker Postgres failed with
  `FATAL: role "assistai" does not exist` — even though the container had just been created with
  that exact user.
- **Root cause:** A separate Postgres (Homebrew) was already listening on `127.0.0.1:5432` and
  `[::1]:5432`. On macOS, `localhost` resolves to the IPv6 loopback `::1` *first*, so our client
  connected to the **local** Postgres (which has no `assistai` role) instead of the container.
  The container's health check passed because `pg_isready` only confirms *a* server is
  answering, not *which* one. *(Two programs were fighting over the same port; the OS handed our
  connection to the wrong one.)*
- **Fix:** Mapped the container to host port **5433** (`"5433:5432"` in `docker-compose.yml`) and
  pointed `DATABASE_URL` / the settings default at `localhost:5433`. This sidesteps the local
  Postgres entirely without touching the user's existing install.
- **Takeaway:** When a dev container's DB "exists but rejects you," check for a host process on
  the same port (`lsof -nP -i :5432`). Pick a non-default host port for project containers to
  avoid clashing with system services.

### C4 · Autogenerated Alembic migration was missing pgvector pieces

- **Symptom:** The migration that Alembic autogenerated would have failed to run: it referenced
  `pgvector.sqlalchemy.vector.VECTOR(...)` for the `chunk.embedding` column but (a) didn't import
  `pgvector`, and (b) never created the `vector` extension the column type depends on.
- **Root cause:** Alembic's autogenerate doesn't know how to emit the import line for a
  third-party column type, and it never emits `CREATE EXTENSION` — extensions live outside its
  model awareness. *(Autogenerate diffs your models against the DB; anything not expressible as a
  table/column diff — like enabling a Postgres extension — it can't see.)*
- **Fix:** Edited the generated migration to (1) add `import pgvector.sqlalchemy`, and (2) run
  `op.execute("CREATE EXTENSION IF NOT EXISTS vector")` as the first statement in `upgrade()`,
  before any table uses the vector type. Re-ran `alembic upgrade head` and confirmed all 13
  tables plus the `vector` extension existed in psql.
- **Takeaway:** Always read autogenerated migrations before applying them (Alembic literally
  prints "please adjust"). Extension setup and custom column-type imports must be added by hand.

### C5 · A stale dev server masked a new route

- **Symptom:** `/health` and `/docs` worked, but `/health/ready` returned `404 Not Found` — even
  though the route was clearly defined and `app.routes` listed it.
- **Root cause:** An older `uvicorn` process from a previous run was still listening on port
  8000. Our new server couldn't bind the port (it silently lost the race), so every request hit
  the **old** process, which predated the `/health/ready` route. *(The code was right; we were
  talking to a stale copy of it.)*
- **Fix:** Killed whatever held the port (`lsof -ti :8000 | xargs kill -9`), restarted uvicorn,
  and confirmed `/health/ready` then returned `{"status":"ready","checks":{"database":true,"redis":true}}`.
- **Takeaway:** When a code change "doesn't take effect," suspect a stale process before
  suspecting the code. Confirm the server you're testing is the one you just started (check the
  port owner, or watch for "address already in use").

### C6 · IDE type-checker can't resolve backend imports

- **Symptom:** The editor flagged `Import "redis"`/`Import "sqlalchemy" could not be resolved`
  (basedpyright) in `app/main.py`, despite the app running and tests passing.
- **Root cause:** uv installs dependencies into `backend/.venv`, but the IDE's type-checker was
  using a different (project-root) Python interpreter that doesn't see that virtualenv. *(The
  warning is about the editor's analysis environment, not the code — the runtime is fine.)*
- **Fix (when it bothers us):** Point the editor's Python interpreter at
  `backend/.venv/bin/python` (e.g. select that interpreter in the IDE, or open the `backend/`
  folder as the workspace root). No code change needed.
- **Takeaway:** "Import could not be resolved" in the editor ≠ a real import error. Verify with
  an actual run/test first; if green, it's an interpreter-selection issue.
